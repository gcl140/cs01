def a(r):
    x = 5
    y = 2
    return 2 * r

def b():
    x = 2
    z = 9
    return z + a(7)

def c(m, n):
    p = b() + a(m) + n
    return p

x = c(2. 3)


xplore = deque()
xplore.append("A")
>>next = xplore.popleft()
is next = goal?
if yes, backchain
otherwise:
add neighbours to explore<<

in bracket - loop

A | B C | D E F | G

create a dictionary 
visited_from = {
    'A' : ""
    'B' : "A"
    'C' : "A"
    'D' : "B"

}

if backchain, is D in visited_from dictionary,
add B to the list, and so on, reverse order, and get the last item